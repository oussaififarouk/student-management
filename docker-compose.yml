services:
  mysql:
    image: mysql:8.4
    container_name: student-management-mysql
    ports:
      - "3306:3306"
    env_file:
      - .env
    environment:
      MYSQL_DATABASE: studentdb
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
